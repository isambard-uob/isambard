import os

from settings import global_settings

def make_ext(modname, pyxfilename):
    from distutils.extension import Extension
    return Extension(name=modname,
                     sources=[pyxfilename],
                     language='c++',
                     include_dirs=[os.path.join(global_settings['package_path'], 'buff')])
